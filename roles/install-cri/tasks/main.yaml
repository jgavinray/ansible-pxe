---
- name: Install docker (until we can put in podman)
  apt:
    name: docker.io
    state: present

- name: Start docker
  systemd:
    state: started
    daemon_reload: yes
    name: docker
    enabled: yes