---
- name: "Install Docker (1/2)"
  apt:
    name: docker.io
    state: present

- name: "Start and enable Docker (2/2)"
  systemd:
    state: started
    daemon_reload: yes
    name: docker
    enabled: yes